name: Update Ruff version
on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-python@v4
    - uses: snok/install-poetry@v1
    - run: poetry install
    - run: poetry run python riff/riff/release/check_ruff_version.py
